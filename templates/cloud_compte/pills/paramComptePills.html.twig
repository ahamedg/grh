    <div class="pill-nav">
        <a class="nav-link active" href="{{ path('categorie_service') }}">Catégorie service</a>
        <a class="nav-link active" href="{{ path('type_service') }}">Type service</a>
        <a class="nav-link active" href="{{ path('type_compte') }}">Type compte</a>
        <a class="nav-link active" href="{{ path('type_budget') }}">Type budget</a>
        <a class="nav-link active" href="{{ path('section') }}">Section</a>
        <a class="nav-link active" href="{{ path('compte') }}">Comptes</a>
        <a class="nav-link active" href="{{ path('sous_compte') }}">Sous-comptes</a>
    </div>
